import 'package:flutter/material.dart';

class InputwardController extends StatelessWidget {
  const InputwardController({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}